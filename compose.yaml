services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: surdev-site:latest
    container_name: surdev-site
    restart: unless-stopped
    init: true
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
    security_opt:
      - no-new-privileges:true
